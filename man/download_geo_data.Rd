% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download_geo_data.R
\name{download_geo_data}
\alias{download_geo_data}
\title{Download GEO Data Resources}
\usage{
download_geo_data(
  gse_id,
  dest_dir = "data/raw",
  overwrite = FALSE,
  log = TRUE,
  log_file = NULL,
  retries = 2,
  timeout = 300
)
}
\arguments{
\item{gse_id}{GEO accession (e.g., "GSE12345").}

\item{dest_dir}{Destination directory for downloaded files (default: "data/raw").}

\item{overwrite}{Logical, whether to overwrite existing files (default: FALSE).}

\item{log}{Logical, whether to record a log file (default: TRUE).}

\item{log_file}{Path to log file. If NULL, auto-generate under "logs/geo/".}

\item{retries}{Number of retry attempts on failure (default: 2).}

\item{timeout}{Timeout (seconds) for each download attempt (default: 300).}
}
\value{
A list containing:
\item{gse_object}{The ExpressionSet object from getGEO.}
\item{supplemental_files}{Vector of paths to downloaded supplemental files.}
\item{platform_info}{List with platform ID and paths to GPL files.}
\item{meta}{Metadata about the download process.}
}
\description{
Downloads GEO data resources including GSEMatrix, supplemental files, and GPL platform files.
Adds support for logging, error handling, metadata recording, timeout control, and retries.
}
\details{
This function retrieves GEO series data (GSEMatrix), downloads associated supplementary files,
and fetches platform information (GPL files). It supports logging progress, retrying on failure,
and setting download timeouts to enhance robustness.
}
\examples{
\dontrun{
# Download a GEO dataset and save to temporary directory
result <- download_geo_data(
  gse_id = "GSE7305",
  dest_dir = tempdir(),
  overwrite = TRUE,
  log = FALSE,
  retries = 2,
  timeout = 300
)

# Access the ExpressionSet object
exprs_data <- Biobase::exprs(result$gse_object)
}
}
