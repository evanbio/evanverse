% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_venn}
\alias{plot_venn}
\title{Venn diagram for 2-4 sets}
\usage{
plot_venn(
  set1,
  set2,
  set3 = NULL,
  set4 = NULL,
  set_names = NULL,
  method = c("classic", "gradient"),
  label = c("count", "percent", "both", "none"),
  palette = NULL,
  return_sets = FALSE
)
}
\arguments{
\item{set1, set2}{Required input vectors (at least two sets).}

\item{set3, set4}{Optional additional sets. Default: \code{NULL}.}

\item{set_names}{Character vector of set labels. If \code{NULL}, uses the
variable names of the inputs. Default: \code{NULL}.}

\item{method}{Drawing method: \code{"classic"} (ggvenn) or \code{"gradient"}
(ggVennDiagram). Default: \code{"classic"}.}

\item{label}{Label type: \code{"count"}, \code{"percent"}, \code{"both"}, or \code{"none"}.
Default: \code{"count"}.}

\item{palette}{For \code{"classic"}: character vector of fill colors, recycled
to match the number of sets. For \code{"gradient"}: a single
\code{RColorBrewer} palette name passed to \code{scale_fill_distiller()}.
If \code{NULL}, defaults are used. Default: \code{NULL}.}

\item{return_sets}{Logical. If \code{TRUE}, returns \code{list(plot, sets)} instead
of just the plot. Default: \code{FALSE}.}
}
\value{
A \code{ggplot} object, or a named list with elements \code{plot} and \code{sets}
if \code{return_sets = TRUE}.
}
\description{
Draws a Venn diagram using either \code{ggvenn} (classic) or \code{ggVennDiagram}
(gradient). Both packages must be installed (they are in \code{Suggests}).
}
\examples{
if (requireNamespace("ggvenn", quietly = TRUE)) {
  set.seed(42)
  plot_venn(sample(letters, 15), sample(letters, 12), sample(letters, 10))
}
}
