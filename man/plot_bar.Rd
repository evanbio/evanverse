% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_bar}
\alias{plot_bar}
\title{Bar plot with optional grouping and sorting}
\usage{
plot_bar(
  data,
  x_col,
  y_col,
  horizontal = FALSE,
  sort = FALSE,
  decreasing = TRUE,
  group_col = NULL,
  sort_by = NULL
)
}
\arguments{
\item{data}{A data.frame.}

\item{x_col}{Character. Column name for the x-axis. Values must be unique
when \code{group_col} is \code{NULL}.}

\item{y_col}{Character. Column name for the y-axis.}

\item{horizontal}{Logical. If \code{TRUE}, flip to horizontal layout. Default: \code{FALSE}.}

\item{sort}{Logical. Whether to sort bars by y values. Default: \code{FALSE}.}

\item{decreasing}{Logical. If \code{sort = TRUE}, sort in decreasing order.
Default: \code{TRUE}.}

\item{group_col}{Character or \code{NULL}. Column name for dodge grouping. Default: \code{NULL}.}

\item{sort_by}{Character or \code{NULL}. Required when \code{sort = TRUE} and \code{group_col}
is set. Must be a valid level of the \code{group_col} column; used to order x
positions by that group's y values.}
}
\value{
A \code{ggplot} object.
}
\description{
Create a bar chart from a data frame with optional dodge grouping,
vertical/horizontal orientation, and sorting by y values.
}
\examples{
df <- data.frame(
  category = c("A", "B", "C", "D"),
  value    = c(10, 25, 15, 30)
)
plot_bar(df, x_col = "category", y_col = "value",
         sort = TRUE, horizontal = TRUE)
}
