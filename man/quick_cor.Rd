% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stat.R
\name{quick_cor}
\alias{quick_cor}
\alias{plot.quick_cor_result}
\title{Correlation analysis with heatmap visualization}
\usage{
quick_cor(
  data,
  vars = NULL,
  method = c("pearson", "spearman", "kendall"),
  use = "pairwise.complete.obs",
  p_adjust_method = c("none", "holm", "hochberg", "hommel", "bonferroni", "BH", "BY",
    "fdr"),
  alpha = 0.05
)

\method{plot}{quick_cor_result}(
  x,
  y = NULL,
  type = c("full", "upper", "lower"),
  show_coef = FALSE,
  show_sig = TRUE,
  hc_order = TRUE,
  hc_method = "complete",
  palette = "gradient_rd_bu",
  lab_size = 3,
  title = NULL,
  show_axis_x = TRUE,
  show_axis_y = TRUE,
  axis_x_angle = 45,
  axis_y_angle = 0,
  axis_text_size = 10,
  sig_level = c(0.001, 0.01, 0.05),
  ...
)
}
\arguments{
\item{data}{A data frame.}

\item{vars}{Character vector of column names to include. \code{NULL}
(default) uses all numeric columns.}

\item{method}{One of \code{"pearson"} (default), \code{"spearman"},
\code{"kendall"}.}

\item{use}{Character. Missing-value handling passed to \code{cor()}.
Default \code{"pairwise.complete.obs"}.}

\item{p_adjust_method}{P-value adjustment method passed to
\code{\link[stats]{p.adjust}}. Default \code{"none"}.}

\item{alpha}{Numeric. Significance threshold for identifying significant
pairs. Default \code{0.05}.}

\item{x}{A \code{quick_cor_result} object from \code{quick_cor()}.}

\item{y}{Ignored.}

\item{type}{One of \code{"full"} (default), \code{"upper"}, \code{"lower"}.}

\item{show_coef}{Logical. Show correlation coefficients? Default \code{FALSE}.}

\item{show_sig}{Logical. Show significance stars? Default \code{TRUE}.
Silently disabled when \code{show_coef = TRUE}.}

\item{hc_order}{Logical. Reorder by hierarchical clustering? Default \code{TRUE}.}

\item{hc_method}{Clustering method. Default \code{"complete"}.}

\item{palette}{evanverse palette name. Default \code{"gradient_rd_bu"}.
\code{NULL} uses a built-in Blue-White-Red scale.}

\item{lab_size}{Numeric. Label size when \code{show_coef = TRUE}. Default \code{3}.}

\item{title}{Character. Plot title. Default \code{NULL}.}

\item{show_axis_x}{Logical. Show x-axis labels? Default \code{TRUE}.}

\item{show_axis_y}{Logical. Show y-axis labels? Default \code{TRUE}.}

\item{axis_x_angle}{Numeric. X-axis label angle. Default \code{45}.}

\item{axis_y_angle}{Numeric. Y-axis label angle. Default \code{0}.}

\item{axis_text_size}{Numeric. Axis text size. Default \code{10}.}

\item{sig_level}{Numeric vector. P-value thresholds for ***, **, *.
Default \code{c(0.001, 0.01, 0.05)}.}

\item{...}{Additional arguments passed to the internal plotting backend.}
}
\value{
An object of class \code{"quick_cor_result"} (invisibly) containing:
\describe{
\item{\code{cor_matrix}}{Correlation coefficient matrix}
\item{\code{p_matrix}}{Unadjusted p-value matrix}
\item{\code{p_adjusted}}{Adjusted p-value matrix; \code{NULL} when
\code{p_adjust_method = "none"}}
\item{\code{method_used}}{Correlation method used}
\item{\code{significant_pairs}}{Data frame of significant pairs}
\item{\code{descriptive_stats}}{Per-variable summary data frame}
\item{\code{params}}{List of input parameters}
\item{\code{data}}{Cleaned data frame (for \code{plot()} method)}
}
Use \code{print(result)} for a one-line summary, \code{summary(result)}
for full details, and \code{plot(result)} for the correlation heatmap.
}
\description{
Computes pairwise correlations with p-values, optional multiple-testing
correction, and a publication-ready heatmap. Supports Pearson, Spearman,
and Kendall methods.
}
\details{
\strong{P-value computation:} Uses \code{psych::corr.test()} when available
(10-100× faster for large matrices); otherwise falls back to a
\code{stats::cor.test()} loop.
}
\examples{
result <- quick_cor(mtcars)
print(result)
summary(result)
plot(result)

result <- quick_cor(
  mtcars,
  vars   = c("mpg", "hp", "wt", "qsec"),
  method = "spearman",
  p_adjust_method = "BH"
)

}
\seealso{
\code{\link[stats]{cor}}, \code{\link[stats]{cor.test}},
\code{\link{quick_ttest}}, \code{\link{quick_chisq}}
}
