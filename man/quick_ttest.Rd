% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stat.R
\name{quick_ttest}
\alias{quick_ttest}
\alias{plot.quick_ttest_result}
\title{Two-group comparison with automatic method selection}
\usage{
quick_ttest(
  data,
  group_col,
  value_col,
  method = c("auto", "t.test", "wilcox.test"),
  paired = FALSE,
  id_col = NULL,
  alternative = c("two.sided", "less", "greater"),
  alpha = 0.05
)

\method{plot}{quick_ttest_result}(
  x,
  y = NULL,
  plot_type = c("boxplot", "violin", "both"),
  add_jitter = TRUE,
  point_size = 2,
  point_alpha = 0.6,
  show_p_value = TRUE,
  p_label = c("p.signif", "p.format"),
  palette = "qual_vivid",
  ...
)
}
\arguments{
\item{data}{A data frame.}

\item{group_col}{Character. Column name for the grouping variable (exactly 2 levels).}

\item{value_col}{Character. Column name for the numeric response variable.}

\item{method}{One of \code{"auto"} (default), \code{"t.test"},
\code{"wilcox.test"}.}

\item{paired}{Logical. Paired test? Default \code{FALSE}.
Requires \code{id_col} when \code{TRUE}.}

\item{id_col}{Character. Column name for the pairing identifier. Required when
\code{paired = TRUE}.}

\item{alternative}{One of \code{"two.sided"} (default), \code{"less"},
\code{"greater"}.}

\item{alpha}{Numeric. Significance level. Default \code{0.05}.}

\item{x}{A \code{quick_ttest_result} object from \code{quick_ttest()}.}

\item{y}{Ignored.}

\item{plot_type}{One of \code{"boxplot"} (default), \code{"violin"},
\code{"both"}.}

\item{add_jitter}{Logical. Add jittered points? Default \code{TRUE}.}

\item{point_size}{Numeric. Jitter point size. Default \code{2}.}

\item{point_alpha}{Numeric. Jitter point transparency (0-1). Default \code{0.6}.}

\item{show_p_value}{Logical. Annotate plot with p-value? Default \code{TRUE}.}

\item{p_label}{One of \code{"p.signif"} (stars, default) or
\code{"p.format"} (numeric).}

\item{palette}{evanverse palette name. Default \code{"qual_vivid"}.
\code{NULL} uses ggplot2 defaults.}

\item{...}{Additional arguments passed to the internal plotting backend.}
}
\value{
An object of class \code{"quick_ttest_result"} (invisibly)
containing:
\describe{
\item{\code{test_result}}{An \code{htest} object from the test}
\item{\code{method_used}}{Character: \code{"t.test"} or
\code{"wilcox.test"}}
\item{\code{descriptive_stats}}{Per-group summary data frame}
\item{\code{normality_tests}}{Shapiro-Wilk results (auto mode only);
\code{NULL} when method is forced}
\item{\code{params}}{List of input parameters}
\item{\code{data}}{Cleaned data frame used for the test (for
\code{plot()} method)}
}
Use \code{print(result)} for a one-line summary, \code{summary(result)}
for full details, and \code{plot(result)} for a comparison plot.
}
\description{
Performs a t-test or Wilcoxon test for exactly two groups, with automatic
method selection based on normality when \code{method = "auto"}.
Always uses Welch's t-test for unpaired comparisons (no equal-variance
assumption).
}
\details{
\strong{Auto method selection logic:}
\itemize{
\item \eqn{n \ge 100}: t-test (CLT applies; Shapiro-Wilk unreliable at large n)
\item \eqn{30 \le n < 100}: Shapiro-Wilk at \eqn{p < 0.01} threshold
\item \eqn{n < 30}: Shapiro-Wilk at \eqn{p < 0.05} threshold
}
}
\examples{
set.seed(42)
df <- data.frame(
  group = rep(c("A", "B"), each = 30),
  value = c(rnorm(30, 5), rnorm(30, 6))
)
result <- quick_ttest(df, group_col = "group", value_col = "value")
print(result)
summary(result)
plot(result)

}
\seealso{
\code{\link[stats]{t.test}}, \code{\link[stats]{wilcox.test}}
}
