% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_pie}
\alias{plot_pie}
\title{Pie chart from a vector or grouped data frame}
\usage{
plot_pie(
  data,
  group_col = NULL,
  count_col = NULL,
  label = c("none", "count", "percent", "both"),
  palette = NULL
)
}
\arguments{
\item{data}{A character/factor vector, named numeric vector, or data.frame.}

\item{group_col}{Character. Column name for group labels (data.frame only).}

\item{count_col}{Character. Column name for counts (data.frame only).
Values must be non-negative.}

\item{label}{Label type: \code{"none"}, \code{"count"}, \code{"percent"}, or \code{"both"}.
Default: \code{"none"}.}

\item{palette}{Character vector or \code{NULL}. Slice fill colors recycled to
match the number of groups. If \code{NULL}, uses ggplot2 default colors.
Default: \code{NULL}.}
}
\value{
A \code{ggplot} object.
}
\description{
Accepts a character/factor vector (frequency counted automatically), a named
numeric vector of pre-computed counts, or a data frame with pre-computed
counts. Slices with zero count are dropped. At least two groups are required.
}
\examples{
# From a vector
plot_pie(c("A", "A", "B", "C", "C", "C"))

# From a data frame
df <- data.frame(group = c("X", "Y", "Z"), count = c(10, 25, 15))
plot_pie(df, group_col = "group", count_col = "count", label = "percent")
}
