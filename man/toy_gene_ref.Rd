% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toy.R
\name{toy_gene_ref}
\alias{toy_gene_ref}
\title{Generate a toy gene reference table}
\usage{
toy_gene_ref(species = c("human", "mouse"), n = 20L)
}
\arguments{
\item{species}{One of \code{"human"} or \code{"mouse"}. Default: \code{"human"}.}

\item{n}{Integer. Number of genes to return. Capped at the 100 available rows.
Default: \code{20}.}
}
\value{
A data.frame with columns \code{symbol}, \code{ensembl_id},
\code{entrez_id}, \code{gene_type}, \code{species}, \code{ensembl_version},
and \code{download_date}.
}
\description{
Generates a small deterministic gene reference table for examples and tests.
The human reference includes the symbols used by \code{\link{toy_gmt}} so
offline GMT parsing and gene ID conversion examples compose without network
access. Not intended for real analyses — use \code{\link{download_gene_ref}}
for a complete reference.
}
\examples{
toy_gene_ref()
toy_gene_ref("mouse", n = 10)
}
